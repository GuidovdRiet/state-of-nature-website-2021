import styled from '@emotion/styled';

export const SliderWrapper = styled.div`
  .slick-track {
    display: flex;
    align-items: center;
    justify-content: center;
  }

  /* the slides */
  .slick-slide {
    margin: 0 27px;
  }
  /* the parent */
  .slick-list {
    overflow: hidden;
  }
`;
